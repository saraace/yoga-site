import styled from '@emotion/styled';

const PlanGroup = styled.div`
    margin: 24px 0;
    width: 370px;

    label{
        margin: 0 0 24px;

        &:last-child{
            margin: 0;
        }
    }
`; 

export default PlanGroup;